#include<stdio.h>
int main()
{
    int t;
    scanf("%d",&t);
    while(t--)
    {
        int a,b;
        int c=0;
        scanf("%d %d",&a,&b);
        if(a%b!=0)
        {
            c+=(b-(a%b));
            a+=(b-(a%b));
        }
        printf("%d\n",c);
    }
    return 0;
}
